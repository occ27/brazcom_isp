# ========================================================================
# Variáveis de Ambiente para Docker Compose
# ========================================================================
# Copie este arquivo para `.env` na raiz do repositório e ajuste os valores
# antes de executar `docker-compose up`

# MySQL - Configurações do Banco de Dados
MYSQL_DATABASE=nfcom
MYSQL_USER=occ
MYSQL_PASSWORD=Altavista740
MYSQL_ROOT_PASSWORD=Altavista740

# Certificados - Diretório no HOST onde os certificados .pfx são armazenados
# DESENVOLVIMENTO LOCAL (Windows): C:\etc\ssl\nfcom
# PRODUÇÃO (Linux): /etc/ssl/nfcom
CERTIFICATES_DIR=/etc/ssl/nfcom

# ========================================================================
# IMPORTANTE: Configurações Adicionais
# ========================================================================
# 
# 1. Backend: Copie `backend/.env.production` para `backend/.env` antes do deploy
# 2. Frontend: A variável REACT_APP_API_BASE_URL está no docker-compose.yml
# 3. Veja CONFIGURACAO_AMBIENTES.md para instruções completas

