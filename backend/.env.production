# ========================================================================
# BACKEND - Variáveis de Ambiente para PRODUÇÃO (Docker)
# ========================================================================
# Use estas configurações quando rodar com docker-compose
# Copie este arquivo para `backend/.env` ao fazer deploy em produção

# Database - Produção (Docker)
# O host 'db' é o nome do serviço MySQL no docker-compose
DATABASE_URL=mysql+pymysql://occ:Altavista740@db:3306/brazcom_db

# Certificados - Produção (Docker/Linux)
# Caminho interno do container onde os certificados são montados
CERTIFICATES_DIR=/etc/ssl/nfcom

# CORS - Produção
# Adicione aqui TODOS os domínios que acessarão a API
# Inclui: domínio de produção, localhost para testes, IP do servidor se necessário
CORS_ORIGINS=https://www.brazcom.com.br,https://brazcom.com.br,http://localhost:3015,http://127.0.0.1:3015

# Security - IMPORTANTE: Altere estas chaves em produção!
SECRET_KEY=MUDE_ESTA_CHAVE_SECRETA_EM_PRODUCAO_USE_SENHA_FORTE_ALEATORIA
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=43200

# NFCom - Ambiente de Transmissão
# ⚠️ CUIDADO: Em produção, use "producao" para emissão REAL de notas fiscais
# Use "homologacao" apenas para testes
NFCOM_AMBIENTE=homologacao
